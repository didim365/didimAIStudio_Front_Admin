import { GetConfigResponse } from "../_api/getConfig";

interface ModelPageProps {
  config: GetConfigResponse;
}

function ModelPage({ config }: ModelPageProps) {
  return <div>상세조회 관련 API 수정 대기 중</div>;
}

export default ModelPage;
