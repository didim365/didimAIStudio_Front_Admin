import { GetCatalogResponse } from "../_api/getCatalog";

interface ModelPageProps {
  catalog: GetCatalogResponse;
}

function ModelPage({ catalog }: ModelPageProps) {
  return <div>상세조회 관련 API 수정 대기 중</div>;
}

export default ModelPage;
